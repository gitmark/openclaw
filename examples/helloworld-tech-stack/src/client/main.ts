/**
 * Main entry point for the client application
 *
 * This file imports our Lit web component, which automatically
 * registers it as a custom HTML element (<chat-app>).
 *
 * Vite will process this file, bundle all dependencies, and
 * serve it with hot module replacement during development.
 */

import './components/chat-app.js';

console.log('🚀 Client application loaded');
console.log('Technologies in use:');
console.log('  - TypeScript: Type-safe code compilation');
console.log('  - Vite: Fast build tool and dev server');
console.log('  - Lit: Web components framework');
